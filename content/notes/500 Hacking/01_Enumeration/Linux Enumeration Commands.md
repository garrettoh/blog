
System info `ls /etc/*-release`
Hostname: `hostname`


User binaries  `ls -lh /usr/bin`
System Binaries  `ls -lh /usr/sbin`

User Enumeration : `cat /etc/passwd`
Who is logged in `who`
Who is logged in and what are they doing: `w`

Who last accessed the system `last`

What DNS server `cat /etc/resolve.conf`

What processes are being run (tree) `ps axjf`
What processes are being run (search) `ps aux | grep -i ''`